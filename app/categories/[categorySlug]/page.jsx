import { AppContext } from '@/app/context/AppContext'
import React, { useContext } from 'react'

const page = () => {
    const {gigs} = useContext(AppContext);
    
  return (
    <div>
      
    </div>
  )
}

export default page
